'use client'
import { uniqueUser } from "@/actions/users";
import { User } from "@/utils/definitions";
import { useEffect, useState } from "react"

export default function Page ({ params }: { params: { utilisateurId: string} }) {
    return <div>{'user'}</div>
}